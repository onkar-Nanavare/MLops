# force re-register
name: Daily Scheduler

on:
  schedule:
    - cron: '15 6 * * *' 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Dummy step
        run: echo "Scheduler fired at 11:15 AM IST"